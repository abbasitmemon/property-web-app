const Footer = () => (
  <footer className="bg-gray-900 text-gray-300">
    <div className="max-w-6xl mx-auto px-4 py-6 text-center text-sm">
      © {new Date().getFullYear()} Property Manager — Laravel + React.
    </div>
  </footer>
);
export default Footer;
